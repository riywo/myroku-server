#!/bin/bash
exec 2>&1
exec \
  envdir ./env \
    sh -c 'cd <%= engine.root %> && \
      exec \
      setuidgid <%= user %> \
      /var/myroku/bin/env bundle exec llenv exec <%= process.command %>
    '
